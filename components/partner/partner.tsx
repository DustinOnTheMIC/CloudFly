import * as React from 'react';

interface PartnerProps {
}

const Partner: React.FunctionComponent<PartnerProps> = (props) => {
  return (
      <>
        
      </>
  );
};

export default Partner;
